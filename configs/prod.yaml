app:
  log_level: WARNING
pipeline:
  batch_size: 50000
source:
  url: https://api.example.com/orders
  retries: 5
  backoff_seconds: 2.0
warehouse:
  db_url: postgresql+psycopg2://analytics:analytics@warehouse:5432/analytics
  table_name: analytics.orders
  if_exists: append
