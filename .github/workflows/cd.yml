name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build and restart pipeline
        run: |
          docker compose pull
          docker compose up --build -d postgres mock-api
          docker compose run --rm pipeline
